# Second Look local release integration — 2026-09-21

Objective: consolidate HAC-67–72, verify the combined result, and integrate verified task-owned work into local `main`. Deployment, cloud configuration, notification activation, real mail, pushes, publication and submission are outside this task. HAC-73 remains the separate release task.

## Repository and release identity

- Integration checkout: `/Users/zaeemkhan/.codex/worktrees/2aa0/agh`, branch `codex/second-look-integration`.
- Starting local `main`: `0aa0be11be7da6215b9dc320456654149e563312`. Original checkout `/Users/zaeemkhan/Documents/agh` is on `codex/hac70` at that commit, with unrelated untracked instruction/skill/configuration files; preserved.
- Combined candidate: `f637cbf` on `codex/hac-72-reminders-digest`. HAC-70 `6b5c4a6` is an ancestor. `git cherry` confirms trust-core `5981578` and `22527d2` are patch-equivalent to included commits. HAC-71 implementation was adapted for notifications; its distinct history must not be blindly merged.
- `de4864456fb21f85af161435ad24f9e2ffa09a75` cherry-picks HAC-71's missing release documentation from `e2c47d7`. The checkpoint is byte-identical to that historical record. It preserves deployed release `848900e` / implementation `c637ba9` and explicitly excludes HAC-72 from that deployment.
- Final verified implementation: `276790ad5fd99e76ff5b359660deeb770436af1c`; UI/revocation correction: `4d9b07c`. The release adds this checkpoint only. Its exact local-main identity/readback is recorded in `/private/tmp/agh-integration-checks/local-main.json` and the HAC-73 handoff comment after fast-forwarding.
- Linear readback: HAC-67–72 Done; HAC-73 Backlog. Historical cloud/provider evidence remains in their individual checkpoints. This combined candidate has **not been deployed**.

## Impact, sequence and recovery

Auth signup/verification → verified family membership → parent mailbox consent/routing and sibling invitation → private board, notes and handled state. Forwarded source → bounded extraction/official evidence → verdict and immutable truthful reply state → reminder eligibility/native scheduling and recipient-specific digest delivery. Public demo sessions reuse the pipeline with explicit provider/delivery exclusions and no private-family access.

Sequence: reconcile ancestry and actual diffs → preserve historical documentation → run required local gates → independent access/invitation/routing and delivery/scheduling reviews → isolated synthetic browser/native runtime checks → resolve findings and rerun affected gates → commit coherent task work → recheck `main` and fast-forward in isolation → record exact release identity in HAC-73.

Ownership: integration root owns Git, checkpoints and final gates; access reviewer owns the small missing revoke-link UI fix and focused regression; delivery reviewer independently inspects immutable replies and notification scheduling; runtime reviewer owns disposable loopback setup and browser evidence. No concurrent writers share a source file.

Recovery: retain candidate branches and old main commit; no history rewrite or source-checkout cleanup. Before local merge, a failure leaves `main` unchanged. After merge, preserve history and use a reviewed revert/correction if necessary. Local Git history is not an off-device backup. Cloud recovery is a separate release decision: preserve additive schema, auth protections, delivery/idempotency rows and existing family activity; do not restore an old database over newer activity.

## Observed verification

Initial consolidated HEAD `de48644`: `npm test` exit 0, 410 tests / 34 files; `npm run typecheck` exit 0; `VITE_CONVEX_URL=http://127.0.0.1:3250 VITE_CONVEX_SITE_URL=http://127.0.0.1:3251 npm run build` exit 0, 98 modules. Fixed v2 code-only evaluator exit 0, all 90 labels pass, `publicationReady=false`. Exact evaluator command: `env -u OPENAI_API_KEY -u FIRECRAWL_API_KEY -u TYPESAFE_API_KEY EVAL_ONLINE=0 EVAL_VERSION=v2 EVAL_OUTPUT=/private/tmp/agh-integration-checks/eval-v2.json node --import tsx evals/run.ts`. Logs and report: `/private/tmp/agh-integration-checks/`.

Package scripts/configuration and Git hooks were inspected: these gates do not deploy, send mail or publish; no active Git hooks or custom hooks path were found. Task-local physical dependencies were copied from the same-lockfile HAC-72 checkout to avoid its documented symlink resolution issue. Historical evaluation files and approved v2 labels remain unchanged.

Review identified a missing product control: `families.revokeLink` was protected and tested, but pending invitation/parent-confirmation rows had no UI caller. A small shared control now uses that existing mutation with per-request pending/error feedback; parent-link revocation coverage supplements the existing invitation regression. Focused family tests (9/9), typecheck and the complete browser revocation checks pass. Browser testing also reproduced a lost reminder-disable confirmation: removing `reminderEmail` from the form key preserves the message while the parent's identity and confirmed-address set still control remounts. The corrected browser rerun verifies the visible notice and rejection of a revoked pending reminder token.

The independent delivery review reproduced a stale-draft failure with provider fetch stubbed: re-extraction changed a matching notice to `cannot_verify` without changing its deadline, but the saved unsent reply retained the old matching text. The isolated failing regression is `/private/tmp/agh-delivery-review/stale-reply.test.ts`. The correction binds composition, saving and send claims to a canonical SHA-256 of the complete reply source. Attempted/uncertain requests retain their exact bytes and are held for reconciliation; accepted receipts remain truthful. A second reviewer identified and verified the fix for a stale worker claiming a newer worker's draft; `beginReply` now requires the caller's source snapshot too. No additional actionable finding remained in the bounded access, source, delivery and scheduling reviews.

Contract changes are internal only: `setReplyDraft` replaces deadline-only arguments with `sourceSnapshot`; `beginReply` also requires it. The single production caller in `reply.ts` and direct callers in reply/demo tests were updated. `getForPipeline` adds a snapshot without removing existing fields; extraction and registry callers remain compatible. Optional `cases.replySourceSnapshot` needs no bulk migration: known-unsent legacy drafts recompose, while unstamped attempted/unknown drafts require reconciliation. The prior test that let a pre-verdict draft authorize a later verdict was strengthened to require fresh composition; invalid saved-text validation remains enforced. No evaluation fixture, label, prompt, gate or historical result changed.

Final implementation gates all exit 0: `npm test` **424 tests / 34 files**; `npm run typecheck`; `VITE_CONVEX_URL=http://127.0.0.1:3250 VITE_CONVEX_SITE_URL=http://127.0.0.1:3251 npm run build` (98 modules); `git diff --check`. Logs: `/private/tmp/agh-integration-checks/final-{tests,typecheck,build}.log`. Focused source-fix review ran 65 reply/notification/demo tests and typecheck successfully. Final source review includes auth, routing, invitations, source trust, attempted-byte preservation, scheduling, consent and demo isolation.

Exact committed replay: `env -u OPENAI_API_KEY -u FIRECRAWL_API_KEY -u TYPESAFE_API_KEY EVAL_ONLINE=0 EVAL_VERSION=v2 EVAL_OUTPUT=/private/tmp/agh-integration-checks/committed-eval-v2.json node --import tsx evals/run.ts`, exit 0 at `276790a`, **90/90 expected labels**, all executable failure categories zero, `publicationReady=false`. Source SHA-256 `1e435eae648440e73232d17e672c1b6e5180d247f00b1146e0204a5e1f10d1f1`; dataset `9a4438e6b10097cca60733ac079441a3df537851de7e354cc7cde9fddd723a8c`. Approved labels and source corpus identities match prior v2. This code-only run establishes no fresh online model/citation/provider or real-world safety result; original v1 history remains intact.

## Integrated browser and native runtime

Fresh loopback backend `127.0.0.1:3250`, HTTP site `3251`, capture sink `3255`, Vite `5184`. Browser plugin/skill absent, so the installed Playwright fallback was used. Desktop 1440×1080 and mobile 390×844 were exercised; screenshots inspected for readable copy, wrapping and overflow. `source-identity.json` records clean tracked source at `276790a` and the tested file hashes. No temporary endpoint was added to tracked source.

- `node /private/tmp/agh-integration-runtime/browser-families.mjs`, final attempt **v3 exit 0**: rendered native signup/verification → family/admin → parent/institution → anonymous explicit mailbox consent → sibling signup/identity-bound invitation → pending invitation and parent-link revocation → timezone, own digest and separate reminder consent/disable. Revoked tokens reject; member administration and anonymous access are denied. Keyboard submission, page identity, nonblank content and no overlay verified; console/page/network/HTTP errors empty. `family-browser-v3/evidence.json` records 10 grouped checks. Attempt 1's incorrect password locator and attempt 2's genuine lost confirmation remain preserved, not relabeled.
- `node /private/tmp/agh-integration-runtime/push-local.mjs`, **exit 0**, installs only to the guarded loopback target. `node /private/tmp/agh-integration-runtime/browser-notifications.mjs`, **exit 0**, runs the native notification harness plus browser actions: signed forward → raw storage → actual durable workflow → source deadline/official evidence → native schedule and capture. **35 grouped checks** pass for two-calendar-day scheduling, recorded timezone/consent, pending-route denial, duplicate webhook, changed/handled/missing/ambiguous/past deadlines, lost acknowledgment, bounded retry, Sunday summary, partial recipient failure and family isolation. Sibling's real keyboard Retry button changes failed → captured with exactly one receipt; normal private note and handled updates appear in the owner browser without navigation/reload. Helper address visible; replies truthfully unsent. `native-notifications-evidence.json` and `notification-browser/evidence.json` record the exact simulated-time/manual calls versus native scheduler behavior. Machine and scheduler clocks were unchanged; the loopback-only notification clock was restored.
- `node /private/tmp/agh-integration-runtime/browser-final-acceptance.mjs`, final **v2 exit 0**, after the source fix: all three synthetic verdicts, unsent drafts, cited evidence, 12 live progress snapshots, separate Sam note/handled updates without reload, visitor isolation, repeat IDs, reset/rerun/cooldown, private sign-in and rejected invalid consent. `final-acceptance-v2/evidence.json` passes. No page/network/HTTP errors; only deliberate cooldown/invalid-token validation console errors. Prior baseline recording remains separate.
- `node /private/tmp/agh-integration-runtime/verify-runtime.mjs`, **exit 0**: all five local endpoints HTTP 200, local capture modes, no external provider keys, synthetic helper/webhook configured and test clock absent. `backend-log-review.json` classifies exact startup/component warnings, also present in the preserved HAC-72 runtime log. Two concurrent-write conflict entries during digest setup recovered: `read-scheduler.mjs` returned all 51 scheduled rows, 34 success / 15 future pending / 2 canceled / 0 failed / 0 in progress. All three surrounding `ensureDigest` jobs succeeded, including the two completions following the conflicts; Sunday schedule and both digest receipts passed. Exact jobs/times are retained in `scheduler-contention-review.json`; no unresolved scheduler failure was found. Captures and silent QA recordings prove local behavior, not real email delivery or a production narration video.

After acceptance, only this task's backend, Vite and capture services were stopped; port readback found no listeners on 3250/3251/3255/5184. During shutdown, a diagnostic exposed the generated disposable backend instance secret in tool output. No cloud/provider credential was involved. The local runtime is retired, its startup is blocked by a retirement marker, and any future run must generate fresh local instance/admin/auth credentials before reuse. Evidence/data/scripts remain available; `shutdown-evidence.json` and the runtime `README.md` record containment without reproducing the value. This does not change the passing source/behavior results.

## HAC-73 release boundary

Deploy the final local `main` release identified in the handoff comment, including implementation `276790a` and this checkpoint. The last recorded development release is HAC-71 `848900e`, which excluded HAC-72; this integration did not reverify cloud release identity. Prior HAC-72's three controlled inbox receipts are transport-only historical evidence; hosted scheduling/recurring delivery is not deployed or activated by this integration. `NOTIFICATION_EMAIL_MODE` remains an independent release gate. HAC-73 must separately prepare/authorize the concrete cloud release and any provider sends, verify the additive schema/source-snapshot behavior and complete its hosted journey; do not reuse local capture or historical provider evidence as new hosted proof. No cloud action, notification activation, real mail, push, publication or submission occurred here.

Skills: Ponytail, frontend-testing-debugging, react-best-practices. No global instruction or memory changes.
