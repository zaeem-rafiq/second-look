# HAC-71 judge demo

Objective: immediate public entry with three synthetic samples, the existing bounded processing path, truthful reply state, isolated repeat/reset behavior and live family coordination.

Worktree: `/Users/zaeemkhan/.codex/worktrees/5d8f/agh`; branch `codex/hac-71-judge-demo`. Base `9f44c50062b1729bc28c142b2dcf91bd5b8d55e9` merges existing HAC-70 `6b5c4a6` and HAC-69 `22527d2` histories without copying other worktrees' changes. Original/shared branches are preserved. No cloud deployment, outbound mail, publication or submission is authorized.

Baseline: `npm test` exit 0, 271 tests; `npm run typecheck` exit 0; `VITE_CONVEX_URL=http://127.0.0.1:3230 npm run build` exit 0. Exact logs under `/private/tmp/agh-hac71-runtime/`. Disposable local Convex uses ports 3230/3231 and no provider keys.

Impact map: public entry → per-visitor demo capability → synthetic family/cases → existing durable verifyCase workflow → deterministic extraction → reviewed seed lookup → shared checks/verdict → unsent reply draft → live query and shared CaseCard. Separate Sam capability permits only same-session notes/handled updates. Private family account, membership, parent consent and inbound sender routes remain unchanged.

Sequence: combined-base checks → isolated demo backend + entry/board polish → behavior tests → local backend/browser run (desktop, mobile, keyboard, error/retry, two participants) → independent authorization/delivery review → final required checks/evals → local commit and Linear evidence. Recovery: task commits and disposable local data; additive optional demo markers preserve existing documents.

Ownership: root owns App/styles/demo UI/checkpoint and final integration; demo_backend owns demo endpoints/schema/provider guards/tests; local_runtime owns ignored runtime setup. HAC-72 owns notification tables/jobs/settings components and will exclude families/cases with `demoSessionId`. Shared changes coordinated through Linear comments and task messages. No notification implementation is part of HAC-71.

Status: acceptance complete locally at implementation commit `c637ba950d8b0ab446a04779ce84d71058070c41`. All required checks and the complete recorded browser run passed. Not deployed to cloud. HAC-73 owns the separately approved hosted release; HAC-72 received the implementation commit for isolated notification integration.

## Contracts and bounds

`demo.start({})` returns 256-bit random Alex/Sam capabilities and an expiry. Only SHA-256 hashes are persisted. `demo.board({token})` exposes only the capability's synthetic family; Sam has separate note/handled authority, while Alex can run/reset. Authors come from the verified capability, never the caller. The scheduled expiry flag invalidates existing subscriptions; mutations also enforce the exact 30-minute deadline. No private API accepts a demo capability.

`demo.runSample({token,sample})` accepts only `suspicious`, `legitimate`, `unverifiable`; stored fixed forwards enter `pipeline.verifyCase`. There is one stored case per sample; repeat returns that case. A failed workflow can restart on the same case only after it is terminal. Reset waits for terminal workflows, removes case/evidence/workflow records and preserves both participant capabilities. Reset is limited to once per minute and three times per session; total runs are limited to 12. Notes: 500 characters each, 20 per case, plus a session collaboration rate limit. Start is globally bounded to 30/hour with burst 10, at most 100 retained sessions; global runs have a 60/minute rate with burst 12. Expiry cleanup retries at most ten times for active jobs, retaining a bounded slot for operator recovery rather than deleting an active workflow.

Optional `families.demoSessionId` and `cases.demoSessionId/demoSample` isolate demo records. `cases.rawStorageId` becomes optional for fixed demo messages only; real inbound still requires it, and private extract/reply paths reject missing storage. All raw-storage consumers were traced. Shared `cases.boardCase(ctx,c)` preserves the original board projection; HAC-72 should add optional reminder projection there and exclude both demo markers from all notification scheduling/delivery.

The demo uses the existing deterministic extraction fallback and reviewed seed registry, with no OpenAI, Firecrawl or AgentMail requests. Shared `cases.beginReply` always leaves a demo draft unsent even with provider credentials; `cases.setReply` rejects sent-state recording for demos. Families have no memberships, registered routes or recipient addresses. The UI labels this preview behavior and distinguishes published-detail matches from sender authentication.

## Verification

- `npm test`: exit 0, 277 tests / 28 files; `npm run typecheck`: exit 0; `VITE_CONVEX_URL=http://127.0.0.1:3230 npm run build`: exit 0; `git diff --check`: exit 0. Logs: `/private/tmp/agh-hac71-runtime/final-{test,typecheck,build}.log`.
- `env -u OPENAI_API_KEY -u FIRECRAWL_API_KEY -u TYPESAFE_API_KEY EVAL_ONLINE=0 EVAL_VERSION=v2 EVAL_OUTPUT=/private/tmp/agh-hac71-runtime/eval-final.json node --import tsx evals/run.ts`: exit 0, all 90 expected labels and all eight executable failure categories pass. Dataset `9a4438e6b10097cca60733ac079441a3df537851de7e354cc7cde9fddd723a8c`. `publicationReady=false`: this is code-only regression evidence, not a fresh online source/model/provider run.
- Six new `convex/demo.test.ts` checks execute the installed durable workflow test component: three verdicts, provider keys configured with a zero-call fetch trap, same-case repeat, separate capability roles, private/cross-session rejection, author forgery rejection, raw-storage failure, quotas, terminal-only reset/retry, expiry and active-workflow cleanup retention. No verdict logic or evaluation labels were changed.
- Independent backend review found no actionable defects; focused auth/reply/provenance/demo run passed 31 tests before the final retry regression was added. Independent frontend review found no actionable routing or mutation-boundary defects. Reviews do not establish hosted delivery.
- Native in-app browser observed an incoming case before verdict, then cited USPS policy mismatch and an explicitly unsent draft. Desktop and 390px mobile inspection led to visible source links even without quotes, readable handled cards, truthful signal wording, shorter mobile navigation, Sam opening directly at the board, and visible reset errors. Date-only labels use UTC to avoid shifting a calendar deadline by viewer timezone.
- Recorded Playwright core run at `http://127.0.0.1:5180`: all samples → live progress/verdict/draft; separate Sam context → keyboard note + handled → Alex updates without reload; separate visitor isolation; same-case repeat; reset clears both boards; rerun creates a new case; reset cooldown error. No page errors, failed requests or HTTP errors. Expected console errors are deliberate reset cooldown and invalid consent-token rejection. `interactive-acceptance/evidence.json` retains `passed=false` because the final invalid-consent harness selector expected the wrong wording; the UI correctly rejected the token. The corrected follow-up run is recorded separately, not relabeled as an earlier pass.

Browser plugin was not listed; the frontend-testing skill's Playwright fallback used bundled dependencies. Native in-app browser was also used for direct visual inspection. Local test scripts, videos, screenshots and machine-readable evidence are under `/private/tmp/agh-hac71-runtime/`; no bearer tokens or provider secrets belong in evidence.

## Final recorded acceptance — 2026-09-21

`node /private/tmp/agh-hac71-runtime/browser-final-acceptance.mjs`: exit 0. `final-acceptance/evidence.json` records `passed=true`, 14:19:03–14:19:40 UTC, desktop 1440×1080 and mobile 390×844, all three expected verdicts and all drafts unsent. Twelve reactive DOM snapshots show receipt/processing, verdict and draft transitions. Sam's separate browser context submits a note with Enter and marks handled; Alex sees both with zero reloads. Fresh visitor sees no cases or Sam note, then only its own sample. Repeat preserves case IDs; reset empties both Alex/Sam boards; a bounded later rerun creates a new case; immediate reset shows the cooldown error inside the viewport. Sign-in remains accessible and invalid parent consent is rejected. Page errors, failed requests and HTTP errors are empty; console errors are only intentionally induced cooldown and invalid-consent rejection.

Readable screenshots: `final-acceptance/mobile-landing-viewport.png`, `mobile-card-viewport.png`, `alex-three-verdicts.png`, `sam-collaboration.png`, `reset-cooldown.png`, `sign-in.png`, `invalid-consent.png`. Main moving recording: `final-acceptance/page@55abaca5fd21e2c3ade3f1c0af1afe4b.webm`; Sam, mobile, independent visitor and private-route videos are indexed in the JSON. This is an automated verification recording, not a polished narrated submission video.

Supplemental `final-polish-v2/evidence.json` proves explicit WebSocket disconnect → reconnect notice → recovered subscription; source links without quote, corrected signal text, correct participant label and Sam auto-scroll also passed. Earlier attempts are retained with their actual failures: wrong consent selector, an offline injection that did not close an established WebSocket, immediate rerun hitting the intended rate limit, and immediate reset hitting the active-workflow guard. The final full recording waits for workflow completion/token refill and passes; no check was weakened to permit a forbidden action.

Post-commit exact evaluator replay with `EVAL_OUTPUT=/private/tmp/agh-hac71-runtime/eval-committed.json` and otherwise the command above: exit 0, 90/90 expected labels, source hash `cdbec0b49e7d1fdec633cb52966a31db486bfda9b635d5aa2a048ea22f904f3a`, same implementation commit and start/end source identity. Final test/typecheck/build logs remain the required passing checks. Authenticated onboarding/parent-consent implementation was preserved and covered by the regression suite; HAC-70's complete real-provider setup run was not repeated or relabeled as HAC-71 evidence. No external providers, cloud deployment, live notification integration or submission were exercised by HAC-71.

Skills: Ponytail; frontend-testing-debugging; react-best-practices (lazy session state, event-owned mutations, primitive effect dependencies). No global instructions or memory rules were changed. Recovery is local Git history plus the disposable loopback runtime; safe deployment preparation is complete, with hosted release authorization/verification left to HAC-73.
