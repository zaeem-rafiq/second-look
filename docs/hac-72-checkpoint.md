# HAC-72 deadline reminders and family digest

Objective: eligible reviewed legitimate notices schedule a parent reminder two calendar days before the date; opted-in authorized members receive a Sunday digest. All dates use an explicitly recorded family timezone. No new cloud deployment or recurring sends are authorized.

Base/worktree: `791c71a`, `/Users/zaeemkhan/.codex/worktrees/9fc6/agh`, branch `codex/hac-72-reminders-digest`. Includes completed HAC-70 `6b5c4a6`, HAC-69 evaluation-only commits (locally `687b687`, `791c71a`), HAC-72 core `5a437bd`, and HAC-71 implementation/evidence (locally `b9df80e`, `eb02c35`). Original/shared checkouts remain untouched.

Baseline: npm test exit 0 (271 tests/27 files); npm run typecheck exit 0; configured loopback npm run build exit 0 (95 modules); fixed offline v2 90-case evaluator exit 0, all failure groups zero, publicationReady=false. Exact logs/report: `/private/tmp/agh-hac72-runtime/baseline-*`.

Impact: extraction/verification/handled state + parent consent + family timezone -> atomic reminder reconciliation -> native scheduled mutation -> one immutable delivery per logical recipient -> leased action -> existing AgentMail client or isolated idempotent loopback capture -> accepted/captured/failed status. Sunday family schedule -> bounded per-parent case summary -> current verified opted-in membership -> separate delivery per member, so partial failure retries only failed recipients. Every send rechecks family/parent/member ownership, consent, deadline/version and demo exclusion. Institution context never supplies official evidence.

Sequence: combined baseline -> scheduling/date helpers and isolated runtime in parallel -> backend/preferences -> component contract to HAC-71 -> focused behavioral tests and local full flow -> independent authorization/scheduling/delivery review -> full checks and eval -> local commit -> bounded real-delivery approval request. Recovery: additive fields/tables, task commits, isolated disposable backend; delivery disabled by default. Provider retries reuse immutable request/key within 23 hours; unknown outcomes outside that window require provider reconciliation, never blind resend.

Coordination: HAC-71 owns App/styles and judge runner; HAC-72 supplies isolated settings/status components. No demo mail is eligible. Conservative policy implemented: separate explicit parent email-link consent, per-member digest opt-in, administrator-saved named timezone; all off by default. No owner answer changed this default.


Pre-integration implementation checks: `npm test` exit 0, 373 tests/33 files; `npm run typecheck` exit 0; configured loopback `npm run build` exit 0; fixed offline v2 all-90 evaluator exit 0 (`preintegration-eval.json`, all failure groups zero, publicationReady=false); `git diff --check` exit 0. UI components are not yet wired in this implementation checkpoint; HAC-71 integration and browser/runtime proof follow.

Independent review found and resolved: at-most-once actions lost before lease claim (bounded recovery crons); local-day late reminder cutoff; synthetic clock permitted only with local capture on loopback; current-source checks on digest dates; member history filtering before its result limit; strict source dates with explicit year and no model overwrite; legacy model-owned dates require source re-extraction. Existing calendar-date display correction is in HAC-71. Source parser deliberately supports ISO and full English month dates in direct due/deadline/pay-by/renew-by clauses; unsupported/conditional/conflicting text stays ambiguous. No guessed numeric or relative dates.

Platform references: [Convex scheduled function error handling](https://docs.convex.dev/scheduling/scheduled-functions#error-handling), [AgentMail idempotent send retention and exact request matching](https://docs.agentmail.to/idempotency). Provider keys expire at 24 hours; our immutable request retries stop earlier (23 hours, or local midnight for reminders). Accepted means provider receipt, not inbox delivery; local capture never means sent mail.

## Combined implementation and verification

HAC-71's shared board serializer now includes the optional reminder projection. Private boards expose notification preferences; the public demo excludes both settings and reminder delivery claims. New notification preferences and optional schema fields preserve existing families with delivery off. Source dates require an explicit `deadlineAmbiguous: false`; legacy model-owned dates remain ineligible until source re-extraction. The only production caller of `setReplyDraft` now supplies its deadline snapshot, and `beginReply` returns the claimed immutable draft. A changed deadline clears only provably unattempted drafts; attempted/accepted/unknown replies preserve exact bytes and show an outdated-date warning. Old composers, stale retries, and threading fallbacks cannot send a replacement under an old key.

Independent review covered notification ownership, consent, idempotency, recovery, source-date parsing, and the reply race. The review found the threading fallback could otherwise send stale text under a new key after an in-flight failure; a shared guard and deferred-response regression resolve it. Final native replay additionally exposed an ordinary account-navigation instruction being mistaken for an alternative deadline. The original fixture was retained, the parser boundary corrected, and a regression added. Conditional, relative, yearless, invalid and conflicting dates still fail closed.

A combined-test dependency problem was reproduced before repair: six demo tests failed through the inherited dependency symlink. A task-local physical copy of the same-lockfile packages resolved it, with SHA-256 comparison proving all 15,588 dependency files/symlinks identical (excluding caches). No tests, mocks, package versions or Vitest configuration were weakened; shared dependency directories were preserved. Evidence is in `/private/tmp/agh-hac72-runtime/dependency-*.json` and logs.

Browser evidence: native sign-in → private board → invalid timezone rejection → keyboard save of America/Chicago → own digest off/on → revoke parent reminders → captured consent request → explicit link acceptance with fragment removal. Invalid consent links reject. The member view exposes only its own digest preference/history, with no parent controls or timezone editor. Desktop and 390px mobile settings/consent were inspected; recipient controls were sized consistently and no horizontal overflow was observed. The integrated legitimate demo completed live processing to an unsent draft with no notification claim. Only expected validation errors appeared in the inspected console. Screenshots and observations: `/private/tmp/agh-hac72-runtime/browser-evidence.json`.

## Remaining release gate and bounded provider check

No HAC-72 cloud deployment, actual provider message or recurring external notification has been authorized or performed. HAC-73 owns the hosted release. The proposed HAC-72 provider check sends exactly three unique, explicitly labelled synthetic messages from the existing `second-look-helper@agentmail.to`: one reminder to `hac70-parent@agentmail.to`, and one digest each to `hac70-admin@agentmail.to` and `hac70-sibling@agentmail.to`. Payloads come from the actual local captures, with a synthetic-test subject/preface and controlled recipient substitution. Repeat only the identical reminder request/key to verify provider idempotency, then inspect the three controlled inboxes for receipt. Existing credentials stay in the one-off process; no cloud environment, webhook, account, billing or recurring configuration changes. Sent mail cannot be recalled; these test inboxes are the entire recipient scope. Approval is required for this new third-party send under the kickoff instructions, which explicitly excluded extending HAC-70's earlier approval.

Operation: cloud notification delivery remains disabled unless `NOTIFICATION_EMAIL_MODE=agentmail` is explicitly approved and configured alongside the existing AgentMail inbox/key. `SETUP_EMAIL_MODE` alone cannot activate reminders/digests. `NOTIFICATION_TEST_NOW` is effective only with loopback local capture; it cannot change hosted or real-provider time. Recovery is to pause the independent notification mode and preserve delivery rows/receipts for reconciliation. Do not delete idempotency history or blindly resend uncertain outcomes beyond their window. No data migration or external-recipient enrollment is performed by deploying the additive schema.

## Final observed local results

- `npm test`: exit 0, **410 tests / 34 files**. `npm run typecheck`: exit 0. `VITE_CONVEX_URL=http://127.0.0.1:3240 VITE_CONVEX_SITE_URL=http://127.0.0.1:3241 npm run build`: exit 0, 98 modules. `git diff --check`: exit 0. Logs: `/private/tmp/agh-hac72-runtime/final-{tests,typecheck,build}.log`.
- `env -u OPENAI_API_KEY -u FIRECRAWL_API_KEY -u TYPESAFE_API_KEY EVAL_ONLINE=0 EVAL_VERSION=v2 EVAL_OUTPUT=/private/tmp/agh-hac72-runtime/final-eval-v2.json node --import tsx evals/run.ts`: exit 0, fixed v2 all-90 expected labels and all executable failure categories pass. Corpus/label hashes and source fingerprint are recorded in the report. `publicationReady=false`: this is offline regression evidence, not fresh online model/source/provider proof.
- `node /private/tmp/agh-hac72-runtime/push-local.mjs`: exit 0, installed combined functions/schema/components only at 127.0.0.1:3240. `HAC72_UI_RETRY=1 node /private/tmp/agh-hac72-runtime/native-notifications.mjs`: exit 0, **33 grouped checks**, run `hac72-c48e081c`. Signed webhook → stored raw forward → actual durable workflow → original newline source deadline → reviewed evidence → scheduled reminder. Simulated notification time exercised native scheduled due dispatch, exactly one capture after a lost acknowledgment, changed-date cancellation and unsent-draft recomposition, handled/missing/ambiguous/past states, Sunday summaries, per-recipient failure, duplicate prevention and cross-family exclusion. The sibling's actual browser retry button completed the failed digest; the runner made no retry mutation and observed one capture. Machine time and the Convex scheduler clock were unchanged; the separate loopback-only notification clock was restored in `finally`.
- Initial native proof and the parser failure are retained as `native-notifications-initial-evidence.json` and `native-notifications-parser-failure-evidence.json`; the latter was not relabelled as a pass. Current proof is `native-notifications-evidence.json`. Local receipt is capture only, never provider acceptance or inbox receipt.
- The bounded provider plan is reviewable at `/private/tmp/agh-hac72-runtime/provider-preview.json`, frozen by `provider-manifest.json`, with runner `provider-verification.ts`. `env -u AGENTMAIL_API_KEY -u AGENTMAIL_INBOX_ID -u NOTIFICATION_EMAIL_MODE node --import tsx /private/tmp/agh-hac72-runtime/provider-verification.ts`: exit 0, three planned messages, zero network requests and no credentials read. Independent read-only review found no actionable plan/allowlist/idempotency-window defects. Actual `--send --verify-received`: **NOT RUN; approval pending**. This transport-only check cannot establish hosted scheduling; HAC-73 still owns that release verification.

Status: implemented and verified locally; pending the bounded actual-provider approval above. Keep HAC-72 In Review until that remaining message-verification gate is evidenced. No cloud rollout or recurring delivery was activated.
